allOf:
  - $ref: ./object.yaml
  - additionalProperties: true
    type: object
    title: Pipeline SSH Key Pair
    description: A Pipelines SSH key pair.
    properties:
      private_key:
        type: string
        description: >-
          The SSH private key. This value will be empty when retrieving the SSH
          key pair.
      public_key:
        type: string
        description: The SSH public key.
